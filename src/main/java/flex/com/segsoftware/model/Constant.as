package com.segsoftware.model
{
	/**
	 * System constants where there is no natural backing entity.
	 */ 
	public class Constant
	{
		/**
		 * We are using Buttons as headers.
		 */ 
		public static var BUTTON_HEADER_HEIGHT:Number = 15;
		
		/**
		 * ControlBars would be height of 25, but we like
		 * 5 for padding on the bottom.
		 */ 
		public static var CONTROLBAR_HEIGHT:Number = 30;
	}
}